{
  "name": "moonstake-rebuild",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Clean-room reverse-engineered implementation of the moonstake.org product model.",
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
